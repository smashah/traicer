---
"@traice-market/traicer": patch
"@traice/desktop": patch
---

Corrected the README and user guides to document only the shipped surface: removed references to unreleased `status`, `stop`, `traces`, and `explore` commands and the unsupported `init --provider` flag, clarified that desktop setup currently requires a Traice Market device credential while CLI capture does not, and pointed all acceptance-evidence claims at one canonical compatibility table.
