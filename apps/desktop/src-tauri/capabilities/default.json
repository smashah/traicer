{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The toolbar has no direct filesystem, network, shell, or process capability.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
